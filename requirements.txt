requests
qbittorrent-api  # Optional: preferred qBittorrent API backend; falls back to raw HTTP requests if missing
PySide6  # Required: primary Qt-based GUI framework
cryptography  # Optional: encrypt credentials in config.ini when available
pytest-qt  # Optional: required for running headless GUI unit tests